# TypeScript-এর any, unknown আর never — মজার উদাহরণে সহজ ব্যাখ্যা

TypeScript শিখতে গেলে তিনটা টাইপ নিয়ে প্রায় সবাই কনফিউজড হয় — **`any`**, **`unknown`**, আর **`never`**। নামগুলো দেখতে একই রকম হলেও কাজ করে পুরো ভিন্নভাবে। এখানে মজার উদাহরণসহ সহজভাবে সবকিছু ব্যাখ্যা করা হলো যাতে গুলিয়ে যাওয়ার আর সুযোগ না থাকে।

---

## ১. `any` — TypeScript-কে ছুটিতে পাঠিয়ে দেওয়া 🏖️

`any` ব্যবহার মানে হলো TypeScript-কে বলা:

> "ভাই, তুমি চেক-শেক বাদ দাও, আমি নিজেই সব দেখছি।"

মানে এই টাইপে যে ভ্যারিয়েবল থাকবে TypeScript সেটার ওপর কোনো টাইপ-চেক করবে না।

### মজার উদাহরণ

```ts
let lokjon: any = "রহিম ভাই";
lokjon = 404; // রহিম ভাই এখন নাম্বার!
lokjon = { pet: "বিড়াল" }; // আবার অবজেক্ট!

lokjon.toLowerCase(); // Error দেখাবে না—কিন্তু রানটাইমে ধরা খাবেন! 💥
```

### কখন ভালো?

- পুরনো জাভাস্ক্রিপ্ট প্রোজেক্ট TypeScript-এ আনতে
- দ্রুত প্রোটোটাইপ বানাতে

### কখন এড়িয়ে চলবেন?

- আসল প্রোডাকশন কোডে

---

## ২. `unknown` — দায়িত্বশীল বড় ভাই 🛡️

`unknown` সবকিছু রাখতে পারে, কিন্তু ব্যবহার করার আগে টাইপ-চেক করতে বাধ্য করে। এটি `any` থেকে অনেক বেশি নিরাপদ।

### মজার উদাহরণ

```ts
let ajkerKhabar: unknown = "কাচ্চি বিরিয়ানি";

// সরাসরি ব্যবহার করতে গেলে Error
ajkerKhabar.slice(0, 3); // ❌

if (typeof ajkerKhabar === "string") {
  console.log(ajkerKhabar.slice(0, 3)); // ✔️ "কাচ"
}

ajkerKhabar = 100;
if (typeof ajkerKhabar === "number") {
  console.log(ajkerKhabar * 2); // ✔️ 200
}
```

### কেন ভালো?

- API / ইউজার ইনপুটের ডাটা হ্যান্ডেল করতে দারুন
- ভুল করার সুযোগ কম

---

## ৩. `never` — যা কখনোই সম্ভব নয় 🚫

`never` মানে এমন কিছু যা কখনো ঘটবে না।

### উদাহরণ ১: অনন্ত লুপ

```ts
function puronoProcrastinator(): never {
  while (true) {
    console.log("আজ না হলে কাল করবো...");
  }
}
```

### উদাহরণ ২: সবসময় error ছোড়া ফাংশন

```ts
function bossToday(mood: string): never {
  throw new Error(`Boss আজ ${mood} মুডে!😨`);
}
```

### উদাহরণ ৩: Exhaustive Checking

```ts
type Khabar = "ভাত" | "রুটি" | "খিচুড়ি";

function daam(k: Khabar) {
  switch (k) {
    case "ভাত":
      return 50;
    case "রুটি":
      return 30;
    case "খিচুড়ি":
      return 70;
    default:
      const impossible: never = k;
      throw new Error(`এটা কী খাবার ভাই? ${impossible}`);
  }
}
```

---

## তিনটার পার্থক্য — এক নজরে 👀

| বিষয়          | any                  | unknown                       | never                                 |
| -------------- | -------------------- | ----------------------------- | ------------------------------------- |
| মান            | যেকোনো কিছু          | যেকোনো কিছু, তবে চেক করতে হবে | কিছুই নয়                             |
| নিরাপত্তা      | কম 🔴                | বেশি 🟢                       | সবচেয়ে কঠোর ⚫                       |
| কোথায় ব্যবহার | দ্রুত কাজ, পুরনো কোড | API/অজানা ডাটা                | এরর, ইনফিনিট লুপ, exhaustive checking |

---

## চূড়ান্ত পরামর্শ 💡

- **any** = TypeScript ঘুমিয়ে আছে 😴
- **unknown** = TypeScript সতর্ক 👀
- **never** = TypeScript বলছে "এটা অসম্ভব!" 🚫

এখন থেকে কোড লেখার সময় কোনটা ব্যবহার করবেন তা নিয়ে আর কনফিউশন হওয়ার কথা নয়। হ্যাপি কোডিং! 🚀

================================================================

# TypeScript-এর Union আর Intersection Types — এমন মজার উদাহরণে শিখুন, ভুলবেন না 🔥

TypeScript-এ এমন দুইটা ম্যাজিক্যাল টাইপ আছে যেগুলো বুঝে ফেললেই মনে হবে—

> “আহা! এতদিন আমি আসলে আধা TypeScript-ই জানতাম!” 😅

এই দুই জাদুর নাম হলো **Union Types (|)** আর **Intersection Types (&)**।

দেখতে দুটোই গাণিতিক চিহ্নের মতো, কিন্তু কাজ করে একদম উল্টো।
চলো মজার, বাস্তব, হালকা হাসির উদাহরণে শিখে ফেলি। 😄

---

## 🥤 Union Types ( | ) — “এটা না হলে ওটা হবে!”

Union মানে: **যে কোনো একটা হলেই চলবে।**

চায়ের দোকানে গেলে আপনি বলতে পারেন:

- "চা দেন"
- "কফি দেন"
- "দুধ-চা দেন"

যেকোনো একটা দিলেই ঠিক আছে।
**এটাই Union।**

---

### ☔ উদাহরণ ১: বৃষ্টির দিনে যাতায়াত

```ts
type Jatra = "হেটে যাবো" | "রিকশা" | "উবার";

function officeJawa(bristiAche: boolean): Jatra {
  if (!bristiAche) return "হেটে যাবো";
  return Math.random() > 0.5 ? "রিকশা" : "উবার";
}

const jatra1 = officeJawa(false); // "হেটে যাবো"
const jatra2 = officeJawa(true); // "রিকশা" অথবা "উবার"
```

---

### ❤️ উদাহরণ ২: Relationship Status

```ts
type Status = "single" | "complicated" | "married";

function checkStatus(name: string): Status {
  if (name === "রহিম") return "complicated";
  if (name === "সুহানা") return "married";
  return "single";
}
```

বাস্তব জীবনে আরো ২১৪৫ ধরনের স্ট্যাটাস থাকে, কিন্তু TypeScript সোজা-সাপটা। 😆

---

### 🔍 Union Type Narrowing

```ts
function printInfo(x: string | number) {
  if (typeof x === "string") {
    console.log("আপনি লিখেছেন:", x.toUpperCase());
  } else {
    console.log("আপনার বয়স:", x * 2);
  }
}
```

---

## 🤝 Intersection Types ( & ) — “সবই লাগবে ভাই!”

Intersection মানে:
**সব টাইপের সব গুণ থাকতে হবে।**

মুদির দোকানে বলছেন—

> "চাল AND ডাল AND তেল দেন"

সব দিতে হবে। একটাও বাদ নয়।
এটাই Intersection।

---

### 🕵️ উদাহরণ ১: Employee + Security Clearance

```ts
type Employee = {
  name: string;
  id: number;
};

type SecurityClearance = {
  badgeLevel: "low" | "medium" | "high";
};

type VIP = Employee & SecurityClearance;

const karim: VIP = {
  name: "কারিম",
  id: 101,
  badgeLevel: "high",
};
```

---

### 🤳 উদাহরণ ২: Influencer + Developer = Devfluencer

```ts
type Influencer = {
  followers: number;
  platform: "YouTube" | "TikTok" | "Facebook";
};

type Developer = {
  skills: string[];
};

type Devfluencer = Influencer & Developer;

const hero: Devfluencer = {
  followers: 50000,
  platform: "YouTube",
  skills: ["JavaScript", "React", "Next.js"],
};
```

---

### 🎒 উদাহরণ ৩: Student + Part-time Job

```ts
type Student = { name: string; dept: string };
type PartTime = { hours: number; salary: number };

type WorkingStudent = Student & PartTime;

const sumon: WorkingStudent = {
  name: "সুমন",
  dept: "CSE",
  hours: 20,
  salary: 8000,
};
```

---

## 🎭 Union vs Intersection — পার্থক্য এক নজরে

| বৈশিষ্ট্য | Union (A       | B)                    | Intersection (A & B) |
| --------- | -------------- | --------------------- | -------------------- |
| মানে      | A অথবা B       | A এবং B               |
| উদাহরণ    | "চা অথবা কফি"  | "চাল AND ডাল AND তেল" |
| প্রয়োজন   | অপশন লাগবে     | সব ফিচার লাগবে        |
| ব্যবহার   | Flexible input | Feature-rich object   |

---

## 🎪 মিক্স উদাহরণ: Parcel Delivery System 📦

```ts
type BasicParcel = {
  id: number;
  weight: number;
};

type Tracking = {
  trackingId: string;
};

type Insurance = {
  insured: boolean;
  amount: number;
};

type Parcel = BasicParcel | (BasicParcel & Tracking & Insurance);

const p1: Parcel = {
  id: 101,
  weight: 2,
};

const p2: Parcel = {
  id: 102,
  weight: 3,
  trackingId: "TK-9988",
  insured: true,
  amount: 5000,
};
```

---

## 💡 সহজ নিয়ম — একেবারে মাথায় বসে যাবে!

### **Union (|) = “অথবা”**

✔ যেকোনো একটা হলেই চলবে।

### **Intersection (&) = “এবং”**

✔ সবগুলো থাকতে হবে।

---

## 🎯 চূড়ান্ত কথা

Union আর Intersection—TypeScript-এর দুই শক্তিশালী অস্ত্র।
ঠিকভাবে ব্যবহার করলে—

- কোড হবে flexible
- Type safety বাড়বে
- বাগ কমবে
- আর আপনি দেখবেন অনেক senior-ই জিজ্ঞেস করছে:
  **“ভাই, এটা কিভাবে লিখলেন?”** 😎

শেষ কথা:

> **Union দিয়ে অপশন বাড়ান
> Intersection দিয়ে পাওয়ার বাড়ান।**

হ্যাপি কোডিং! 🚀🎉
